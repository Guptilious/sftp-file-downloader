# sftp-file-downloader
Download files located on an SFTP server with a specific naming format. Credentials are passed using a .netrc file (example shown below).

# Packages
`sudo apt isntall lftp`

# ~/.netrc
```
machine sftp.example.com
login your_username
password your_password
```
